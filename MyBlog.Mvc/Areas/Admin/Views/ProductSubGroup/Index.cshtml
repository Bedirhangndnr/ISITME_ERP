@using MyBlog.Shared.Utilities.Results.ComplexTypes;
@model MyBlog.Entities.Dtos.ProductSubGroupDtos.ProductSubGroupListDto
@{
    Layout = "_Layout";
    ViewBag.Title = "Hasta Referansları";
}

<ol class="breadcrumb mb-r mt-2">
    <li class="breadcrumb-item"> <a asp-area="Admin" asp-controller="Home" asp-action="Index">Admin</a></li>
    <li class="breadcrumb-item active"><a asp-area="Admin" asp-controller="Brand" asp-action="Index">MArkalar</a></li>
</ol>
@if (Model.ResultStatus == ResultStatus.Success)
{
    <div id="modalPlaceHolder" aria-hidden="true"></div>
    <div class="card mb-4 mt-2">
        <div class="card-header">
            <i class="fas fa-table mr-1"></i>
            Ürün Alt Grupları
        </div>
        <div class="card-body">
            <div class="spinner-border" role="status" style="display: none;">
                <span class="sr-only">Yükleniyor...</span>
            </div>
            <div class="table-responsive">
                <table class="table table-bordered datatable" id="productSubGroupsTable" width="100%" cellspacing="0">
                    <thead>
                        <tr>
                            <th>ID</th>
                            <th>Başlık</th>
                            <th>Açıklama</th>
                            <th class="dt-button-col">İşlemler</th>
                        </tr>
                    </thead>
                    <tfoot>
                        <tr>
                            <th>ID</th>
                            <th>Başlık</th>
                            <th>Açıklama</th>
                            <th class="dt-button-col">İşlemler</th>
                        </tr>
                    </tfoot>
                    <tbody>
                        @foreach (var subGroup in Model.ProductSubGroups)
                        {
                            <tr name="@subGroup.Id">
                                <td>@subGroup.Id</td>
                                <td>@subGroup.Title</td>
                                <td>@subGroup.Description</td>
                                <td class="dt-button-col">
                                    <button title="Güncelle" class="btn btn-primary btn-sm btn-update" data-id="@subGroup.Id">
                                        <span class="fas fa-edit"></span>
                                    </button>
                                    <button title="Sil" class="btn btn-danger btn-sm btn-delete" data-id="@subGroup.Id">
                                        <span class="fas fa-minus-circle"></span>
                                    </button>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
}
@if (Model.ResultStatus == ResultStatus.Error)
{
    <div class="alert alert-danger mt-3">
        @Model.Message <br />
        Dashboard sayfasına geri dönmek için lütfen <a class="alert-link" asp-area="Admin" asp-controller="customerType" asp-action="Index">tıklayınız.</a>
    </div>
}

@section Scripts
    {
    <script src="~/AdminArea/js/productSubGroupIndex.js" type="application/ecmascript"></script>
    @*    <script>
        $(document).ready(function () {
            $('#btnAdd').click(function () {
                window.location.href = '@Url.Action("Add", "Customer")';
            });

        });
    </script>*@
}

